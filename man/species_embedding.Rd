% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/species-embedding.R
\name{species_embedding}
\alias{species_embedding}
\title{Species embedding}
\usage{
species_embedding(
  x,
  method = "abund_pmi",
  dim = 64L,
  weighting = NULL,
  factorization = NULL,
  time = NULL,
  min_occurrence = 5L,
  min_cooccurrence = 1L,
  glove_iter = 20L
)
}
\arguments{
\item{x}{A \code{specvec_data} object.}

\item{method}{One of \code{\link[=specvec_methods]{specvec_methods()}}; default \code{"abund_pmi"}. Pass \code{NULL}
to drive the engine purely from \code{weighting} + \code{factorization}.}

\item{dim}{Embedding dimension.}

\item{weighting}{Optional weighting name overriding the method's.}

\item{factorization}{Optional factorization name overriding the method's.}

\item{time}{Optional time window: fit on plots whose stored \code{time} falls in
the window. Numeric time selects the closed interval \verb{[min(time), max(time)]}; otherwise set membership. \code{NULL} (default) uses all plots. For
trajectories comparable across windows, use \code{\link[=species_trajectory]{species_trajectory()}}.}

\item{min_occurrence}{Drop species occurring in fewer than this many plots.}

\item{min_cooccurrence}{Keep a species pair only if it co-occurs in at least
this many plots.}

\item{glove_iter}{Iterations for the GloVe factorizer.}
}
\value{
A \code{specvec_embedding}: species x dim matrix \code{V} (species row names)
plus method, capability, and preprocessing metadata.
}
\description{
Learn species vectors from co-occurrence. \code{method} is sugar for a registered
\verb{(weighting, factorization)} pair; pass \code{weighting}/\code{factorization} to
override for power use. The default \code{"abund_pmi"} is abundance-weighted PMI.
}
\examples{
df <- data.frame(plot = rep(paste0("p", 1:6), each = 2),
                 species = c("A","B","A","B","A","C","B","C","A","B","B","C"))
x <- specvec(df, "plot", "species")
species_embedding(x, method = "pmi", dim = 2, min_occurrence = 1)
}
