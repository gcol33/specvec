% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{compare_embeddings}
\alias{compare_embeddings}
\alias{specvec_benchmark}
\title{Benchmark embedding methods}
\usage{
compare_embeddings(
  x,
  methods = c("ca", "pmi", "abund_pmi", "glove"),
  metrics = c("cooc_ppmi", "cooc_raw", "link_auc", "eunis", "trait"),
  dim = 64L,
  seeds = 1:3,
  test_frac = 0.2,
  min_occurrence = 5L,
  min_cooccurrence = 1L,
  labels = NULL,
  traits = NULL,
  reference = "ca",
  glove_iter = 20L,
  control = .benchmark_control()
)

specvec_benchmark(
  x,
  methods = c("ca", "pmi", "abund_pmi", "glove"),
  metrics = c("cooc_ppmi", "cooc_raw", "link_auc", "eunis", "trait"),
  dim = 64L,
  seeds = 1:3,
  test_frac = 0.2,
  min_occurrence = 5L,
  min_cooccurrence = 1L,
  labels = NULL,
  traits = NULL,
  reference = "ca",
  glove_iter = 20L,
  control = .benchmark_control()
)
}
\arguments{
\item{x}{A \code{specvec_data} object.}

\item{methods}{Method names to compare (see \code{\link[=specvec_methods]{specvec_methods()}}). \code{"glove"}
is skipped with a note if \code{text2vec} is not installed.}

\item{metrics}{Which metrics to report: any of \code{"cooc_ppmi"}, \code{"cooc_raw"},
\code{"link_auc"}, \code{"eunis"}, \code{"trait"}. EUNIS/trait skip cleanly when their
inputs are absent.}

\item{dim}{Embedding dimension.}

\item{seeds}{Integer seeds; each gives an independent split. Reported as
across-seed mean and SD.}

\item{test_frac}{Held-out plot fraction (and species fraction for traits).}

\item{min_occurrence, min_cooccurrence}{Species/pair filters, applied globally
before splitting.}

\item{labels}{Plot-level label for the EUNIS metric: a column name in
\code{x$labels}, a plot-ordered vector, or \code{NULL} to use the first label column.}

\item{traits}{Species-level numeric attributes for the trait metric: a matrix
or data frame with species row names, or \code{NULL} to skip.}

\item{reference}{Method the verdict compares against (default \code{"ca"}).}

\item{glove_iter}{Iterations for the GloVe factorizer.}

\item{control}{Scale guards and kNN neighbourhood; see the defaults in the
source. Override only for very large data.}
}
\value{
A \code{specvec_benchmark}: a tidy \verb{method x metric} summary (mean, sd, n),
the per-seed raw rows, the run config, and the fixed-criterion verdict. A
method "beats" the reference only if it exceeds it on both neutral metrics
(\code{cooc_raw} and \code{link_auc}) by more than two pooled SDs.
}
\description{
Score registered embedding methods on neutral ecological tasks under one fair
protocol: a plot-level train/test split, one scored species-pair set per seed,
never-seen negatives for link prediction, and a single presence-mean community
definition shared across methods. Co-occurrence metrics need only \code{x}; the
EUNIS and trait metrics activate when labels or a trait table are supplied.
The species set is filtered once (\code{min_occurrence}) before splitting, so every
method sees the same species. Runs on any user dataset, not just the reference
data that tuned the package default.
}
\examples{
df <- data.frame(
  plot = rep(paste0("p", 1:40), each = 3),
  species = sample(paste0("s", 1:15), 120, replace = TRUE),
  cover = round(runif(120, 1, 100), 1)
)
x <- specvec(df, "plot", "species", abundance = "cover")
\donttest{
compare_embeddings(x, methods = c("ca", "pmi", "abund_pmi"),
                   dim = 4, seeds = 1:2, min_occurrence = 1)
}
}
