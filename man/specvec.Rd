% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/specvec-data.R
\name{specvec}
\alias{specvec}
\title{Build a specvec data object}
\usage{
specvec(
  data,
  plot,
  species,
  abundance = NULL,
  time = NULL,
  labels = NULL,
  cover_scale = c("percent", "proportion", "braun_blanquet"),
  duplicates = c("max", "sum", "first", "error"),
  cover_mapping = NULL
)
}
\arguments{
\item{data}{A data frame in long form (one row per plot-species record).}

\item{plot}{Name of the plot/site id column.}

\item{species}{Name of the species column.}

\item{abundance}{Optional name of the cover/abundance column.}

\item{time}{Optional name of a plot-level time column (e.g. decade or year),
stored for windowed embeddings and trajectories.}

\item{labels}{Optional character vector of plot-level label column names
(carried for the benchmark; the core embedding never needs them).}

\item{cover_scale}{How to read \code{abundance}: \code{"percent"} (default),
\code{"proportion"}, or \code{"braun_blanquet"}. See \code{\link[=cover_from_scale]{cover_from_scale()}}.}

\item{duplicates}{How to aggregate duplicated \verb{plot x species} rows:
\code{"max"} (default), \code{"sum"}, \code{"first"}, or \code{"error"}.}

\item{cover_mapping}{Optional named numeric vector overriding an ordinal
cover-scale lookup.}
}
\value{
A \code{specvec_data} object: sparse \code{P} (presence), \code{COV} (cover or
\code{NULL}), sorted \code{species}/\code{plots} id maps, optional \code{time}/\code{labels}, \code{meta}.
}
\description{
Turn a long \verb{plot, species (, abundance)} table into the sparse matrices the
embedding engine needs. Column roles are passed by name. Abundance, time,
and labels are optional. A stored \code{time} column powers the windowed
embeddings and trajectories (\code{\link[=species_trajectory]{species_trajectory()}}, \code{\link[=community_trajectory]{community_trajectory()}}).
}
\examples{
df <- data.frame(
  plot = c("p1","p1","p2","p2","p3","p3"),
  species = c("A","B","A","B","A","C"),
  cover = c(40, 10, 60, 5, 30, 80)
)
specvec(df, plot = "plot", species = "species", abundance = "cover")
}
