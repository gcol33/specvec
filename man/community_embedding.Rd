% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/community.R
\name{community_embedding}
\alias{community_embedding}
\title{Community embedding}
\usage{
community_embedding(
  x,
  method = "abund_pmi",
  dim = 64L,
  weights = c("cover", "presence"),
  embedding = NULL,
  normalize = FALSE,
  time = NULL,
  min_occurrence = 5L,
  min_cooccurrence = 1L,
  ...
)
}
\arguments{
\item{x}{A \code{specvec_data} object.}

\item{method}{Embedding method (see \code{\link[=specvec_methods]{specvec_methods()}}); ignored if
\code{embedding} is supplied.}

\item{dim}{Embedding dimension; ignored if \code{embedding} is supplied.}

\item{weights}{\code{"cover"} (default when cover is present) or \code{"presence"}.}

\item{embedding}{Optional pre-fitted \code{specvec_embedding} to pool, instead of
fitting one here.}

\item{normalize}{If \code{TRUE}, L2-normalize each plot vector after pooling.}

\item{time}{Optional time window (see \code{\link[=species_embedding]{species_embedding()}}): pool (and, when
fitting, train on) only plots in the window. \code{NULL} (default) uses all
plots. For per-window communities in one shared frame, use
\code{\link[=community_trajectory]{community_trajectory()}}.}

\item{min_occurrence, min_cooccurrence}{Passed to \code{\link[=species_embedding]{species_embedding()}} when
fitting.}

\item{...}{Passed to \code{\link[=species_embedding]{species_embedding()}}.}
}
\value{
A \code{specvec_community}: plot x dim matrix \code{U} (plot row names) plus
pooling and provenance metadata.
}
\description{
Place each plot in the species embedding space by pooling the vectors of the
species it contains. The readout is uniform across methods (pooled species
vectors), so plot embeddings stay comparable. \code{weights = "cover"} pools by
cover (the abundance moat at plot level); \code{weights = "presence"} is the plain
mean of present species vectors.
}
\examples{
df <- data.frame(plot = rep(paste0("p", 1:8), each = 2),
  species = c("A","B","A","B","A","C","B","C","A","B","B","C","A","C","A","B"),
  cover = c(80,20,50,50,60,40,50,50,70,30,40,60,90,10,55,45))
x <- specvec(df, "plot", "species", abundance = "cover")
community_embedding(x, method = "abund_pmi", dim = 3, min_occurrence = 1)
}
