% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{species_trajectory}
\alias{species_trajectory}
\title{Species trajectory through a fixed embedding frame}
\usage{
species_trajectory(
  x,
  species,
  frame = NULL,
  by = NULL,
  method = "abund_pmi",
  dim = 64L,
  weights = c("cover", "presence"),
  min_occurrence = 5L,
  min_cooccurrence = 1L,
  frame_embedding = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{specvec_data} object built with a \code{time} column.}

\item{species}{Character vector of focal species ids to trace.}

\item{frame}{Optional character vector of species defining the fixed frame;
default is every species except \code{species}.}

\item{by}{Window definition passed to the time splitter: \code{NULL} (default) for
one window per distinct time value, or a numeric break vector to bin time.}

\item{method}{Frame embedding method (see \code{\link[=specvec_methods]{specvec_methods()}}).}

\item{dim}{Frame embedding dimension.}

\item{weights}{\code{"cover"} (default when cover is present) weights co-occurrence
by the geometric mean of covers, matching AbundPMI; \code{"presence"} counts
co-occurring plots.}

\item{min_occurrence, min_cooccurrence}{Frame species/pair filters.}

\item{frame_embedding}{Optional pre-fitted \code{specvec_embedding} to reuse as the
frame instead of fitting one (its species become the frame).}

\item{...}{Passed to \code{\link[=species_embedding]{species_embedding()}} when fitting the frame.}
}
\value{
A \code{specvec_trajectory}: \code{U} (focal x window x dim array, \code{NA} where a
focal species has no co-occurrence in a window), \code{support} (focal x window
count of window plots containing the focal species), the fixed \code{frame}
embedding, the \code{windows} table, and pooling provenance.
}
\description{
Track focal species through time without the cross-time alignment problem.
A single \emph{frame} embedding is fitted once (the stable coordinate system), and
each focal species is then placed, per time window, at the cover-weighted
centroid of the frame species it co-occurs with in that window. Because every
window is read out in one fixed frame, the trajectory points are directly
comparable across time -- no per-window rotation, no Procrustes alignment, no
circularity. This is the engine behind alien-integration trajectories (v0.3).
}
\details{
The frame defaults to every species except the focal ones, so the focal
species move through the background community rather than helping define it.
Pass \code{frame} to fix the frame to a reference set (e.g. native species only).
}
\examples{
set.seed(1)
df <- data.frame(
  plot = rep(paste0("p", 1:60), each = 3),
  species = sample(c("focal", paste0("s", 1:12)), 180, replace = TRUE),
  decade = rep(c(1990, 2000, 2010), each = 60)
)
x <- specvec(df, "plot", "species", time = "decade")
tr <- species_trajectory(x, species = "focal", dim = 4, min_occurrence = 1)
as.data.frame(tr)
}
\seealso{
\code{\link[=community_trajectory]{community_trajectory()}}, \code{\link[=species_embedding]{species_embedding()}}
}
