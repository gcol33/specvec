% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{community_trajectory}
\alias{community_trajectory}
\title{Community trajectory and novelty over time}
\usage{
community_trajectory(
  x,
  by = NULL,
  reference = NULL,
  method = "abund_pmi",
  dim = 64L,
  weights = c("cover", "presence"),
  normalize = FALSE,
  k = 5L,
  min_occurrence = 5L,
  min_cooccurrence = 1L,
  frame_embedding = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{specvec_data} object built with a \code{time} column.}

\item{by}{Window definition (see \code{\link[=species_trajectory]{species_trajectory()}}).}

\item{reference}{Reference window the novelty is measured against: \code{NULL}
(default) uses the first window, a window label or index picks one, or a
\code{specvec_community}/matrix supplies an external baseline.}

\item{method, dim, min_occurrence, min_cooccurrence}{Frame embedding controls.}

\item{weights}{\code{"cover"} (default when present) or \code{"presence"} pooling.}

\item{normalize}{If \code{TRUE}, L2-normalize each community vector before scoring.}

\item{k}{Neighbours averaged in the novelty distance.}

\item{frame_embedding}{Optional pre-fitted frame embedding to reuse.}

\item{...}{Passed to \code{\link[=species_embedding]{species_embedding()}} when fitting the frame.}
}
\value{
A \code{specvec_community_trajectory}: \code{communities} (per-window plot x dim
matrices in the shared frame), a \code{novelty} table (window, center, n_plots,
mean and median per-plot novelty vs the reference), the fixed \code{frame}, and
the resolved \code{reference} label.
}
\description{
Embed each time window's communities in one fixed frame and score how novel
they are relative to a reference window. The species frame is fitted once on
all plots, so the per-window community embeddings live in the same space and
novelty is comparable across time (the temporal counterpart of
\code{\link[=community_novelty]{community_novelty()}}).
}
\examples{
set.seed(1)
df <- data.frame(
  plot = rep(paste0("p", 1:90), each = 3),
  species = sample(paste0("s", 1:15), 270, replace = TRUE),
  decade = rep(c(1990, 2000, 2010), each = 90)
)
x <- specvec(df, "plot", "species", time = "decade")
community_trajectory(x, dim = 4, min_occurrence = 1, k = 3)
}
\seealso{
\code{\link[=species_trajectory]{species_trajectory()}}, \code{\link[=community_novelty]{community_novelty()}}
}
