% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nearest.R
\name{nearest_species}
\alias{nearest_species}
\title{Nearest species}
\usage{
nearest_species(emb, species, n = 10L, metric = c("cosine", "euclidean"))
}
\arguments{
\item{emb}{A \code{specvec_embedding}.}

\item{species}{Focal species id (a row name of the embedding).}

\item{n}{Number of neighbours to return.}

\item{metric}{\code{"cosine"} (default) or \code{"euclidean"}.}
}
\value{
A tidy data frame ranked nearest-first, with a \code{species} column and a
\code{similarity} column (cosine) or \code{distance} column (euclidean).
}
\description{
Rank the species closest to a focal species in the embedding. The sticky
demo function: ask which species an embedding places next to Robinia.
}
\examples{
df <- data.frame(plot = rep(paste0("p", 1:8), each = 2),
  species = c("A","B","A","B","A","C","B","C","A","B","B","C","A","C","A","B"))
emb <- species_embedding(specvec(df, "plot", "species"),
                         method = "pmi", dim = 3, min_occurrence = 1)
nearest_species(emb, "A", n = 2)
}
