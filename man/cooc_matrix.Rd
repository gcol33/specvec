% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cooccurrence.R
\name{cooc_matrix}
\alias{cooc_matrix}
\title{Co-occurrence operator (engine primitive)}
\usage{
cooc_matrix(
  x,
  weighting = c("counts", "ppmi", "abundance_pmi", "chi_square"),
  min_occurrence = 5L,
  min_cooccurrence = 1L,
  time = NULL
)
}
\arguments{
\item{x}{A \code{specvec_data} object.}

\item{weighting}{One of \code{"counts"}, \code{"ppmi"}, \code{"abundance_pmi"},
\code{"chi_square"}.}

\item{min_occurrence}{Drop species occurring in fewer than this many plots.}

\item{min_cooccurrence}{Keep a species pair only if it co-occurs in at least
this many plots (species x species weightings only).}

\item{time}{Optional time window: restrict to plots whose stored \code{time} falls
in the window before building the operator. Numeric time selects the closed
interval \verb{[min(time), max(time)]}; otherwise set membership. \code{NULL} uses all
plots.}
}
\value{
A sparse matrix: species x species for \code{counts}/\code{ppmi}/
\code{abundance_pmi}, or plot x species for \code{chi_square}.
}
\description{
Build the weighted species x species operator a factorization would consume
(for \code{chi_square}, return the plot x species contingency, whose residual is
applied implicitly at factorization). Exported for power users; most callers
use \code{\link[=species_embedding]{species_embedding()}}.
}
\examples{
df <- data.frame(plot = c("p1","p1","p2","p2","p3","p3"),
                 species = c("A","B","A","B","A","C"))
x <- specvec(df, "plot", "species")
cooc_matrix(x, "ppmi", min_occurrence = 1)
}
